/**
 * This is the main dashboard page inside the admin dashboard.
 */

import { Box, Typography } from "@mui/material";

const Dashboard = () => {
  return (
    <Box>
      <Typography variant="h1">Comming Soon</Typography>
    </Box>
  );
};
export default Dashboard;
